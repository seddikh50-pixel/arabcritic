import { Button } from "@/components/ui/button"
import React from 'react'

const RegisterBtn = () => {
    return (
        <div className="h-full">
            <Button className={"bg-green-600 text-lg h-full"}>تسجيل</Button>
        </div>
    )
}

export default RegisterBtn
